<div class="section-head">
  {{#if eyebrow}}<div class="eyebrow">{{eyebrow}}</div>{{/if}}
  <h2 class="section-heading{{#if small}} section-heading--sm{{/if}}"{{#if id}} id="{{id}}"{{/if}}>{{{heading}}}</h2>
  {{#if lede}}<p class="section-head__lede">{{{lede}}}</p>{{/if}}
</div>
