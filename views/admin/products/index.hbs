<div class="flex items-center justify-between">
  <h1 class="font-display text-2xl font-medium text-al-navy">Products</h1>
  <a href="/admin/products/new" class="btn-primary !px-4 !py-2 text-sm">New Product</a>
</div>

<div class="mt-6 overflow-hidden rounded-none border border-al-line bg-white">
  <table class="w-full text-left text-sm">
    <thead class="bg-al-panel text-xs font-semibold uppercase tracking-wide text-al-ink/60">
      <tr>
        <th class="px-4 py-3">Name</th>
        <th class="px-4 py-3">Slug</th>
        <th class="px-4 py-3">Price</th>
        <th class="px-4 py-3">Status</th>
        <th class="px-4 py-3"></th>
      </tr>
    </thead>
    <tbody class="divide-y divide-al-line">
      {{#each products}}
      <tr>
        <td class="px-4 py-3 font-semibold text-al-navy">{{this.name}}</td>
        <td class="px-4 py-3 text-al-ink/60">{{this.slug}}</td>
        <td class="px-4 py-3">{{currency this.price}}</td>
        <td class="px-4 py-3">{{#if this.active}}<span class="tag-pill">Active</span>{{else}}<span class="tag-pill">Inactive</span>{{/if}}</td>
        <td class="px-4 py-3 text-right">
          <a href="/admin/products/{{this._id}}/edit" class="text-xs font-semibold uppercase tracking-wide text-al-navy hover:underline">Edit</a>
        </td>
      </tr>
      {{/each}}
    </tbody>
  </table>
</div>
